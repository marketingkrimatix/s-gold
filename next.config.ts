import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // To export as static site, uncomment the following lines:
  // output: 'export',
  // // Optional: unoptimized images are often required for static hosting
  // images: { unoptimized: true }, 
  //npm run build: This will automatically generate the static files in a folder named `out`
  /* config options here */
};

export default nextConfig;
